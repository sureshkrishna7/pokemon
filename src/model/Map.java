package model;

public class Map {

  private Items[][] board;

  public Map() {
	 // Constructor to initialize all the GroundObjects
	 // Create the base map
	 // Create Safari map;

	 /* Each Item should have DYNAMIC INITIALIZATION,
	  * For example: A grass object should be initialized as "Items a = new Grass()"
	  * A tree object should be initialized as "Items b = new Tree()"
	  */
  }
}
